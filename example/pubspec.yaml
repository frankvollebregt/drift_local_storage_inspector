name: example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  drift_local_storage_inspector:
    path: ../
  storage_inspector: ^0.5.0
  drift: ^2.32.0

dev_dependencies:
  build_runner: ^2.13.1
  drift_dev: ^2.32.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
